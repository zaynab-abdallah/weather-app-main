# 🚀 دليل التشغيل السريع

## لديك خياران:

---

## ✅ الخيار 1: النسخة العادية (HTML/CSS/JS) - الأسهل

### التشغيل:
1. افتح ملف `index.html` مباشرة في المتصفح
2. لا يحتاج تثبيت أي شيء!

### المزايا:
- ✅ سهل وسريع
- ✅ لا يحتاج Node.js
- ✅ يعمل مباشرة

### الملفات المطلوبة:
- `index.html`
- `styles.css`
- `script.js`
- `assets/` (الصور والخطوط)

---

## 🔥 الخيار 2: Next.js (React + TypeScript) - للمحترفين

### المتطلبات:
1. Node.js 18+ مثبت

### التشغيل:
```bash
# 1. تثبيت المكتبات
npm install

# 2. تشغيل المشروع
npm run dev

# 3. افتح المتصفح على
http://localhost:3000
```

### المزايا:
- ✅ TypeScript (أمان من الأخطاء)
- ✅ React Components (منظمة ومعاد استخدامها)
- ✅ أداء أفضل
- ✅ SEO محسّن
- ✅ سهولة التوسع

### الملفات المطلوبة:
- `app/` (Next.js pages)
- `components/` (React components)
- `lib/` (API & utilities)
- `types/` (TypeScript types)
- `public/assets/` (الصور والخطوط)

---

## ⚠️ ملاحظة مهمة للنسخة العادية

إذا كنت تستخدم `index.html` وظهر خطأ:

### الحل:
1. اضغط `Ctrl + Shift + R` لتحديث بدون كاش
2. افتح DevTools (F12) وتحقق من Console
3. تأكد من وجود ملفات `assets/images/` و `assets/fonts/`

---

## 🎯 التوصية

**للتعلم والبساطة**: استخدم النسخة العادية (`index.html`)

**للمشاريع الاحترافية والتطوير**: استخدم Next.js

---

## 📞 المساعدة

إذا واجهت مشاكل:
1. تحقق من وجود جميع الملفات
2. افتح Console في المتصفح (F12)
3. تأكد من الاتصال بالإنترنت (للـ API)

التطبيق جاهز للاستخدام! 🎉

